.DS_Store
Thumbs.db
*.swp
*~
.vscode/
.idea/
node_modules/
arcade/private/config.json
arcade/admin/config.json
arcade/admin/api-keys.json
arcade/admin/github-token.json
**/AGENTS.md
wiki/
wiki-old-backup/
archive/more/contributors.html
archive/more/video.html
mcp-server/.venv/
__pycache__/
drafts/

# NOTE: arcade/shared/adenosine-*.js are generated by `npm run build:adenosine`
# but are deliberately COMMITTED, not ignored. GitHub Pages serves this repo's
# branch directly (there is no Pages build workflow), so an untracked bundle
# would 404 on magmacrunch.com. Re-run the script after changing a dependency.
mcp-server/.env

# Chat/admin shared secret. Lives at ~/arcade-secrets.env on the Pi (outside the
# rsynced tree); never commit a local copy.
*secrets.env
