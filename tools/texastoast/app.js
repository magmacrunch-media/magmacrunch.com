// ============================================================
// texastoast Playground — run the Python RPG engine in-browser
// ============================================================
// Pyodide (Python WASM) runs the real texastoast package. A fake
// tkinter module (tt_tk_shim.py) forwards drawing to __ttHost,
// which paints a retained display list onto the <canvas> below.
// Games are driven by after() -> setTimeout, keys by keydown/keyup
// on the focused canvas.

(async () => {
  // ----------------------------------------------------------
  // Constants
  // ----------------------------------------------------------

  const PYODIDE_CDN = "https://cdn.jsdelivr.net/pyodide/v0.25.1/full/pyodide.js";
// BEGIN GENERATED TEXASTOAST FILES
// Generated by scripts/sync-texastoast-playground.py from the installed
// texastoast package — do not edit by hand.
// 26 modules
const TT_FILE_CONTENTS = {
  "__init__.py": "\"\"\"texastoast \u2014 Python RPG engine with I2C hardware abstraction.\"\"\"\n\n__version__ = \"0.3.0\"\n\n\ndef __getattr__(name):\n    if name in (\"Config\", \"GameLoop\", \"Game\"):\n        from texastoast.core import Config, Game, GameLoop\n        return {\"Config\": Config, \"GameLoop\": GameLoop, \"Game\": Game}[name]\n\n    if name in (\"CanvasRenderer\", \"Camera\", \"SpriteSheet\"):\n        from texastoast.render import Camera, CanvasRenderer, SpriteSheet\n        return {\"CanvasRenderer\": CanvasRenderer, \"Camera\": Camera, \"SpriteSheet\": SpriteSheet}[name]\n\n    if name in (\"TileMap\", \"Entity\", \"AABB\"):\n        from texastoast.world import AABB, Entity, TileMap\n        return {\"TileMap\": TileMap, \"Entity\": Entity, \"AABB\": AABB}[name]\n\n    if name in (\"InputState\", \"KeyboardInput\", \"MagmaHubInput\", \"CompositeInput\"):\n        from texastoast.input import CompositeInput, InputState, KeyboardInput, MagmaHubInput\n        return {\"InputState\": InputState, \"KeyboardInput\": KeyboardInput,\n                \"MagmaHubInput\": MagmaHubInput, \"CompositeInput\": CompositeInput}[name]\n\n    if name in (\"I2CBus\", \"MagmaHub\", \"ControllerState\"):\n        from texastoast.i2c import ControllerState, I2CBus, MagmaHub\n        return {\"I2CBus\": I2CBus, \"MagmaHub\": MagmaHub, \"ControllerState\": ControllerState}[name]\n\n    if name in (\"DialogueBox\", \"Menu\", \"HUD\"):\n        from texastoast.ui import HUD, DialogueBox, Menu\n        return {\"DialogueBox\": DialogueBox, \"Menu\": Menu, \"HUD\": HUD}[name]\n\n    raise AttributeError(f\"module 'texastoast' has no attribute {name!r}\")\n\n\n__all__ = [\n    \"Config\",\n    \"GameLoop\",\n    \"Game\",\n    \"CanvasRenderer\",\n    \"Camera\",\n    \"SpriteSheet\",\n    \"TileMap\",\n    \"Entity\",\n    \"AABB\",\n    \"InputState\",\n    \"KeyboardInput\",\n    \"MagmaHubInput\",\n    \"CompositeInput\",\n    \"I2CBus\",\n    \"MagmaHub\",\n    \"ControllerState\",\n    \"DialogueBox\",\n    \"Menu\",\n    \"HUD\",\n]\n",
  "core/__init__.py": "from texastoast.core.config import Config\n\n__all__ = [\"Config\", \"GameLoop\", \"Game\"]\n\n\ndef __getattr__(name):\n    if name == \"GameLoop\":\n        from texastoast.core.loop import GameLoop\n        return GameLoop\n    if name == \"Game\":\n        from texastoast.core.game import Game\n        return Game\n    raise AttributeError(f\"module {__name__!r} has no attribute {name!r}\")\n",
  "core/config.py": "from __future__ import annotations\n\nfrom dataclasses import dataclass\n\n\n@dataclass\nclass Config:\n    title: str = \"texastoast\"\n    width: int = 640\n    height: int = 480\n    fps: int = 30\n    tile_size: int = 16\n    bg_color: str = \"#1a1a2e\"\n    grid_color: str = \"#16213e\"\n    debug: bool = False\n\n\nDEFAULT_CONFIG = Config()\n",
  "core/game.py": "from __future__ import annotations\n\nimport logging\nimport tkinter as tk\nfrom collections.abc import Callable\n\nfrom texastoast.core.config import DEFAULT_CONFIG, Config\nfrom texastoast.core.loop import GameLoop\n\nlogger = logging.getLogger(__name__)\n\n\nclass Game:\n    \"\"\"Main game class. Manages the tkinter window and game lifecycle.\"\"\"\n\n    def __init__(\n        self,\n        title: str = DEFAULT_CONFIG.title,\n        width: int = DEFAULT_CONFIG.width,\n        height: int = DEFAULT_CONFIG.height,\n        fps: int = DEFAULT_CONFIG.fps,\n        config: Config | None = None,\n        root: tk.Misc | None = None,\n        max_consecutive_errors: int = 10,\n    ):\n        \"\"\"Create the game window.\n\n        ``root`` accepts an existing tkinter root or frame to build into, for\n        embedding the game in a larger app or for tests. When omitted a new\n        ``Tk`` window is created and owned by this Game.\n\n        ``max_consecutive_errors`` is how many back-to-back failing frames the\n        loop tolerates before it stops the game and re-raises from\n        :meth:`start`; 0 disables the cutoff and restores the old\n        log-and-continue behaviour.\n        \"\"\"\n        self.config = config or Config(title=title, width=width, height=height, fps=fps)\n        self._max_consecutive_errors = max_consecutive_errors\n        self._owns_root = root is None\n        self._root = root if root is not None else tk.Tk()\n        if self._owns_root:\n            self._root.title(self.config.title)\n            self._root.resizable(False, False)\n\n        self.canvas = tk.Canvas(\n            self._root,\n            width=self.config.width,\n            height=self.config.height,\n            bg=self.config.bg_color,\n            highlightthickness=0,\n        )\n        self.canvas.pack()\n\n        self._loop: GameLoop | None = None\n        self._update_fn: Callable[[float], None] | None = None\n        self._render_fn: Callable[[], None] | None = None\n        self._teardown: list[Callable[[], None]] = []\n        self._loop_error: BaseException | None = None\n\n        # Without this, closing the window with the X button leaves the loop\n        # running with a pending after() callback. Applies to any window with a\n        # close button, not just one we created; an embedded Frame has no\n        # protocol() and needs no handler.\n        if hasattr(self._root, \"protocol\"):\n            self._root.protocol(\"WM_DELETE_WINDOW\", self.quit)\n\n    @property\n    def root(self) -> tk.Misc:\n        return self._root\n\n    @property\n    def loop(self) -> GameLoop | None:\n        return self._loop\n\n    def set_update(self, fn: Callable[[float], None]):\n        self._update_fn = fn\n\n    def set_render(self, fn: Callable[[], None]):\n        self._render_fn = fn\n\n    def start(self):\n        \"\"\"Start the loop and block in the tkinter main loop.\n\n        When an external ``root`` was supplied the caller owns the main loop,\n        so only the game loop is started here.\n        \"\"\"\n        update = self._update_fn or (lambda dt: None)\n        render = self._render_fn or (lambda: None)\n        self._loop_error = None\n        self._loop = GameLoop(\n            self._root, update, render, self.config.fps,\n            max_consecutive_errors=self._max_consecutive_errors,\n            on_error=self._on_loop_error,\n        )\n        self._loop.start()\n        if self._owns_root:\n            self._root.mainloop()\n\n        # tkinter swallows exceptions raised inside an after() callback, so a\n        # broken update() would otherwise end the game with no indication of\n        # why. Carrying it out here is what lets a caller \u2014 a test, or a script\n        # host like magmascript \u2014 report it against its own source.\n        if self._loop_error is not None:\n            error, self._loop_error = self._loop_error, None\n            raise error\n\n    def _on_loop_error(self, exc: BaseException):\n        self._loop_error = exc\n        self.quit()\n\n    def on_close(self, fn: Callable[[], None]):\n        \"\"\"Register a cleanup callback to run on :meth:`quit`.\n\n        Useful for input sources and other objects holding tkinter bindings,\n        e.g. ``game.on_close(keyboard.destroy)``.\n        \"\"\"\n        self._teardown.append(fn)\n\n    def quit(self):\n        if self._loop:\n            self._loop.stop()\n\n        for fn in self._teardown:\n            try:\n                fn()\n            except Exception:\n                logger.exception(\"Exception in teardown callback\")\n        self._teardown.clear()\n\n        # A root we did not create belongs to the caller; leave it standing.\n        if not self._owns_root:\n            return\n\n        try:\n            self._root.quit()\n            self._root.destroy()\n        except tk.TclError:\n            pass\n\n    def bind_key(self, key: str, callback: Callable):\n        self._root.bind(key, callback)\n\n    def bind_key_release(self, key: str, callback: Callable):\n        if key.startswith(\"<\") and key.endswith(\">\"):\n            inner = key[1:-1]\n            self._root.bind(f\"<KeyRelease-{inner}>\", callback)\n        else:\n            self._root.bind(f\"<KeyRelease-{key}>\", callback)\n",
  "core/loop.py": "from __future__ import annotations\n\nimport logging\nimport time\nfrom collections.abc import Callable\n\nlogger = logging.getLogger(__name__)\n\n\nclass GameLoop:\n    \"\"\"Tick-based game loop using tkinter's after() for scheduling.\"\"\"\n\n    MAX_DT = 0.1  # clamp dt to prevent physics explosions on large gaps\n\n    def __init__(\n        self,\n        root,\n        update_fn: Callable[[float], None],\n        render_fn: Callable[[], None],\n        fps: int = 30,\n        max_consecutive_errors: int = 10,\n        on_error: Callable[[BaseException], None] | None = None,\n    ):\n        self._root = root\n        self._update_fn = update_fn\n        self._render_fn = render_fn\n        self._interval_ms = max(1, int(1000 / fps))\n        self._max_consecutive_errors = max_consecutive_errors\n        self._on_error = on_error\n        self._consecutive_errors = 0\n        self._error: BaseException | None = None\n        self._running = False\n        self._last_time = 0.0\n        self._frame_count = 0\n        self._fps_display = 0.0\n        self._fps_timer = 0.0\n        self._after_id = None\n\n    @property\n    def fps(self) -> float:\n        return self._fps_display\n\n    @property\n    def error(self) -> BaseException | None:\n        \"\"\"The exception that stopped the loop, if one did.\"\"\"\n        return self._error\n\n    @property\n    def frame_count(self) -> int:\n        return self._frame_count\n\n    def start(self):\n        if self._running:\n            return\n        self._running = True\n        self._last_time = time.monotonic()\n        self._fps_timer = self._last_time\n        self._frame_count = 0\n        self._consecutive_errors = 0\n        self._error = None\n        self._tick()\n\n    def stop(self):\n        self._running = False\n        if self._after_id is not None:\n            try:\n                self._root.after_cancel(self._after_id)\n            except Exception:\n                # Root may already be torn down; nothing left to cancel.\n                pass\n            self._after_id = None\n\n    def _tick(self):\n        if not self._running:\n            return\n\n        now = time.monotonic()\n        dt = min(now - self._last_time, self.MAX_DT)\n        self._last_time = now\n\n        try:\n            self._update_fn(dt)\n            # update() may have quit the game \u2014 a menu's Quit item, a win\n            # condition, a script calling quit(). Rendering after that draws\n            # onto a destroyed canvas.\n            if self._running:\n                self._render_fn()\n        except Exception as exc:\n            # A loop that logs and carries on turns one broken callback into\n            # thousands of identical tracebacks a second while the game looks\n            # alive but does nothing. Tolerate a transient failure; give up on\n            # a persistent one.\n            self._consecutive_errors += 1\n            if (self._max_consecutive_errors > 0\n                    and self._consecutive_errors >= self._max_consecutive_errors):\n                logger.error(\n                    \"Stopping the game loop after %d consecutive errors\",\n                    self._consecutive_errors,\n                )\n                self._error = exc\n                self.stop()\n                if self._on_error is not None:\n                    # The owner takes it from here \u2014 typically by ending the\n                    # main loop and re-raising from start().\n                    try:\n                        self._on_error(exc)\n                    except Exception:\n                        logger.exception(\"Exception in game loop error handler\")\n                    return\n                # Nothing is listening. Re-raising here does not reach the\n                # caller \u2014 tkinter catches it inside the after() callback and\n                # hands it to report_callback_exception \u2014 but that is what\n                # prints the traceback, which beats losing it entirely.\n                raise\n            if self._consecutive_errors == 1:\n                logger.exception(\"Exception in game loop update/render\")\n            else:\n                # The same error, again. One traceback per streak is enough;\n                # repeating it 30 times a second buries everything else.\n                logger.error(\n                    \"Exception in game loop update/render (%d in a row): %r\",\n                    self._consecutive_errors, exc,\n                )\n        else:\n            self._consecutive_errors = 0\n\n        self._frame_count += 1\n        if now - self._fps_timer >= 1.0:\n            self._fps_display = self._frame_count / (now - self._fps_timer)\n            self._frame_count = 0\n            self._fps_timer = now\n\n        if not self._running:  # stop() may have been called from update/render\n            return\n\n        # Subtract the time update/render just spent, otherwise every frame\n        # costs interval + work and the target fps is never reached.\n        work_ms = (time.monotonic() - now) * 1000.0\n        delay = max(1, int(self._interval_ms - work_ms))\n        self._after_id = self._root.after(delay, self._tick)\n",
  "i2c/__init__.py": "from texastoast.i2c.bus import I2CBus\nfrom texastoast.i2c.hub import MagmaHub\nfrom texastoast.i2c.protocol import (\n    BUTTONS_ADDR,\n    CONTROLLER_SIZE,\n    JOYSTICK_ADDR,\n    ControllerState,\n)\n\n__all__ = [\n    \"I2CBus\",\n    \"MagmaHub\",\n    \"ControllerState\",\n    # Protocol constants are re-exported so callers can address the hub\n    # without reaching into texastoast.i2c.protocol.\n    \"CONTROLLER_SIZE\",\n    \"BUTTONS_ADDR\",\n    \"JOYSTICK_ADDR\",\n]\n",
  "i2c/bus.py": "\"\"\"I2C bus abstraction with smbus2 + graceful fallback.\n\nIf smbus2 is not installed (e.g., on macOS or non-RPi systems),\na mock bus is used that returns dummy data. Games can still run\nwith keyboard input.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport logging\n\nlogger = logging.getLogger(__name__)\n\ntry:\n    from smbus2 import SMBus\n    HAS_SMBUS = True\nexcept ImportError:\n    HAS_SMBUS = False\n    logger.info(\"smbus2 not available \u2014 I2C hardware disabled\")\n\n\nclass I2CBus:\n    \"\"\"Wrapper around smbus2 with error handling and mock fallback.\"\"\"\n\n    def __init__(self, bus_number: int = 1):\n        self._bus_number = bus_number\n        self._bus: object | None = None\n        self._mock = not HAS_SMBUS\n        self._open()\n\n    def _open(self):\n        if self._mock:\n            logger.warning(f\"I2C bus {self._bus_number} running in MOCK mode\")\n            return\n        try:\n            self._bus = SMBus(self._bus_number)\n            logger.info(f\"I2C bus {self._bus_number} opened\")\n        except FileNotFoundError:\n            logger.warning(f\"I2C bus {self._bus_number} not found \u2014 falling back to mock\")\n            self._mock = True\n        except PermissionError:\n            logger.warning(f\"I2C bus {self._bus_number} permission denied \u2014 falling back to mock\")\n            self._mock = True\n        except OSError as e:\n            logger.warning(f\"I2C bus {self._bus_number} error: {e} \u2014 falling back to mock\")\n            self._mock = True\n\n    @property\n    def is_mock(self) -> bool:\n        return self._mock\n\n    @property\n    def bus_number(self) -> int:\n        return self._bus_number\n\n    def read_byte_data(self, address: int, register: int) -> int | None:\n        \"\"\"Read one byte. Returns ``None`` if the read failed or the bus is mock.\n\n        A failed read must be distinguishable from a device that genuinely\n        reported 0x00, otherwise callers cannot tell whether hardware is\n        present at all.\n        \"\"\"\n        if self._mock:\n            return None\n        try:\n            return self._bus.read_byte_data(address, register)\n        except OSError as e:\n            logger.debug(f\"I2C read error at 0x{address:02x}: {e}\")\n            return None\n\n    def write_byte_data(self, address: int, register: int, value: int):\n        if self._mock:\n            return\n        try:\n            self._bus.write_byte_data(address, register, value)\n        except OSError as e:\n            logger.debug(f\"I2C write error at 0x{address:02x}: {e}\")\n\n    def read_i2c_block_data(self, address: int, register: int,\n                            length: int) -> list[int] | None:\n        \"\"\"Read a block of bytes. Returns ``None`` if the read failed or the\n        bus is mock. See :meth:`read_byte_data` for why.\"\"\"\n        if self._mock:\n            return None\n        try:\n            return self._bus.read_i2c_block_data(address, register, length)\n        except OSError as e:\n            logger.debug(f\"I2C block read error at 0x{address:02x}: {e}\")\n            return None\n\n    def write_i2c_block_data(self, address: int, register: int, data: list[int]):\n        if self._mock:\n            return\n        try:\n            self._bus.write_i2c_block_data(address, register, data)\n        except OSError as e:\n            logger.debug(f\"I2C block write error at 0x{address:02x}: {e}\")\n\n    def scan(self, start: int = 0x03, end: int = 0x77) -> list[int]:\n        \"\"\"Scan I2C bus for devices. Returns list of responding addresses.\"\"\"\n        if self._mock:\n            return []\n        found = []\n        for addr in range(start, end + 1):\n            try:\n                self._bus.read_byte(addr)\n                found.append(addr)\n            except OSError:\n                pass\n        return found\n\n    def close(self):\n        if self._bus is not None:\n            try:\n                self._bus.close()\n            except Exception:\n                pass\n            self._bus = None\n\n    def __enter__(self):\n        return self\n\n    def __exit__(self, *args):\n        self.close()\n",
  "i2c/hub.py": "\"\"\"I2C hub abstraction \u2014 reads controller state from I2C slave devices.\"\"\"\n\nfrom __future__ import annotations\n\nimport logging\nimport time\n\nfrom texastoast.i2c.bus import I2CBus\nfrom texastoast.i2c.protocol import (\n    CONTROLLER_SIZE,\n    DEFAULT_HUB_ADDRESSES,\n    ControllerState,\n)\n\nlogger = logging.getLogger(__name__)\n\n\nclass MagmaHub:\n    \"\"\"Represents one Magma Hub (Pico I2C slave) with N controllers.\"\"\"\n\n    def __init__(\n        self,\n        address: int,\n        bus: I2CBus,\n        num_controllers: int = 1,\n        poll_interval: float = 0.016,\n    ):\n        self._address = address\n        self._bus = bus\n        self._num_controllers = num_controllers\n        self._poll_interval = poll_interval\n        self._states: list[ControllerState] = [\n            ControllerState() for _ in range(num_controllers)\n        ]\n        self._last_poll = 0.0\n        self._connected = False\n\n    @property\n    def address(self) -> int:\n        return self._address\n\n    @property\n    def num_controllers(self) -> int:\n        return self._num_controllers\n\n    @property\n    def connected(self) -> bool:\n        \"\"\"Whether the last :meth:`poll` actually read a controller.\n\n        ``False`` until a successful poll, and ``False`` again as soon as reads\n        start failing or the bus is mock.\n        \"\"\"\n        return self._connected\n\n    def poll(self) -> list[ControllerState]:\n        \"\"\"Read all controller states from the hub.\n        Uses the protocol: write [start_addr, num_bytes], then read.\"\"\"\n        now = time.monotonic()\n        if now - self._last_poll < self._poll_interval:\n            return self._states\n\n        self._last_poll = now\n\n        any_ok = False\n        for i in range(self._num_controllers):\n            try:\n                start_addr = i * CONTROLLER_SIZE\n                data = self._read_block(start_addr, CONTROLLER_SIZE)\n                if data is not None and len(data) >= CONTROLLER_SIZE:\n                    self._states[i] = ControllerState(\n                        buttons=data[0],\n                        joystick=data[1],\n                    )\n                    any_ok = True\n            except Exception as e:\n                logger.debug(f\"Hub 0x{self._address:02x} ctrl {i} read error: {e}\")\n\n        # Connected if *any* controller answered. Assigned once, after the loop:\n        # assigning per-controller let the last one overwrite the others.\n        self._connected = any_ok\n        return self._states\n\n    def get_controller(self, index: int) -> ControllerState:\n        if 0 <= index < self._num_controllers:\n            return self._states[index]\n        return ControllerState()\n\n    def _read_block(self, start_addr: int, num_bytes: int) -> list[int] | None:\n        \"\"\"Read a block from the hub using the magma protocol.\n\n        Returns ``None`` when the read fails or the bus is mock, so that\n        :meth:`poll` can tell \"no hardware\" apart from \"all buttons released\".\n        \"\"\"\n        if self._bus.is_mock:\n            return None\n\n        try:\n            # Protocol: write [start_addr, num_bytes], then read\n            self._bus.write_i2c_block_data(\n                self._address, start_addr, [num_bytes]\n            )\n            time.sleep(0.001)\n            return self._bus.read_i2c_block_data(\n                self._address, start_addr, num_bytes\n            )\n        except Exception as e:\n            logger.debug(f\"I2C read block error: {e}\")\n            return None\n\n    @classmethod\n    def scan_buses(cls, bus_numbers: list[int] = None,\n                   addresses: list[int] = None,\n                   num_controllers: int = 1) -> list[MagmaHub]:\n        \"\"\"Scan I2C buses and create MagmaHub instances for found devices.\"\"\"\n        if bus_numbers is None:\n            bus_numbers = [1]\n        if addresses is None:\n            addresses = DEFAULT_HUB_ADDRESSES\n\n        hubs = []\n        for bus_num in bus_numbers:\n            bus = I2CBus(bus_num)\n            if bus.is_mock:\n                logger.info(f\"Bus {bus_num} is mock \u2014 skipping scan\")\n                bus.close()\n                continue\n\n            found = bus.scan()\n            logger.info(f\"Bus {bus_num} scan: {[f'0x{a:02x}' for a in found]}\")\n\n            claimed = False\n            for addr in found:\n                if addr in addresses:\n                    hub = cls(addr, bus, num_controllers=num_controllers)\n                    hubs.append(hub)\n                    claimed = True\n                    logger.info(f\"Found Magma Hub at 0x{addr:02x}\")\n\n            # Only the hubs we return keep the bus alive; otherwise close it.\n            if not claimed:\n                bus.close()\n\n        return hubs\n",
  "i2c/protocol.py": "\"\"\"I2C protocol constants and controller state parsing.\"\"\"\n\nfrom __future__ import annotations\n\nfrom dataclasses import dataclass\n\n# \u2500\u2500 Memory map per controller \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nBUTTONS_ADDR = 0x00\nJOYSTICK_ADDR = 0x01\nCONTROLLER_SIZE = 2  # bytes per controller\n\n# \u2500\u2500 Button bitmasks (active high) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nBTN_UP    = 0b00000001\nBTN_DOWN  = 0b00000010\nBTN_LEFT  = 0b00000100\nBTN_RIGHT = 0b00001000\nBTN_A     = 0b00010000\nBTN_B     = 0b00100000\nBTN_START = 0b01000000\nBTN_SELECT = 0b10000000\n\n# \u2500\u2500 Default I2C addresses \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\nDEFAULT_HUB_ADDRESSES = [0x08, 0x09, 0x0A, 0x0B]\n\n\n@dataclass\nclass ControllerState:\n    \"\"\"Parsed state from one controller.\"\"\"\n    buttons: int = 0\n    joystick: int = 0\n\n    @property\n    def up(self) -> bool:\n        return bool(self.buttons & BTN_UP)\n\n    @property\n    def down(self) -> bool:\n        return bool(self.buttons & BTN_DOWN)\n\n    @property\n    def left(self) -> bool:\n        return bool(self.buttons & BTN_LEFT)\n\n    @property\n    def right(self) -> bool:\n        return bool(self.buttons & BTN_RIGHT)\n\n    @property\n    def a(self) -> bool:\n        return bool(self.buttons & BTN_A)\n\n    @property\n    def b(self) -> bool:\n        return bool(self.buttons & BTN_B)\n\n    @property\n    def start(self) -> bool:\n        return bool(self.buttons & BTN_START)\n\n    @property\n    def select(self) -> bool:\n        return bool(self.buttons & BTN_SELECT)\n\n    def direction(self) -> tuple[float, float]:\n        dx = 0.0\n        dy = 0.0\n        if self.left:\n            dx -= 1.0\n        if self.right:\n            dx += 1.0\n        if self.up:\n            dy -= 1.0\n        if self.down:\n            dy += 1.0\n        return dx, dy\n",
  "input/__init__.py": "from texastoast.input.abstract import InputSource, InputState\n\n__all__ = [\"InputState\", \"InputSource\", \"KeyboardInput\", \"MagmaHubInput\", \"CompositeInput\"]\n\n\ndef __getattr__(name):\n    if name == \"KeyboardInput\":\n        from texastoast.input.keyboard import KeyboardInput\n        return KeyboardInput\n    if name == \"MagmaHubInput\":\n        from texastoast.input.magma_hub import MagmaHubInput\n        return MagmaHubInput\n    if name == \"CompositeInput\":\n        from texastoast.input.magma_hub import CompositeInput\n        return CompositeInput\n    raise AttributeError(f\"module {__name__!r} has no attribute {name!r}\")\n",
  "input/abstract.py": "from __future__ import annotations\n\nfrom dataclasses import dataclass\nfrom typing import Protocol\n\n\n@dataclass\nclass InputState:\n    \"\"\"Unified controller state for all input methods.\"\"\"\n    up: bool = False\n    down: bool = False\n    left: bool = False\n    right: bool = False\n    a: bool = False\n    b: bool = False\n    start: bool = False\n    select: bool = False\n\n    @property\n    def dx(self) -> float:\n        result = 0.0\n        if self.left:\n            result -= 1.0\n        if self.right:\n            result += 1.0\n        return result\n\n    @property\n    def dy(self) -> float:\n        result = 0.0\n        if self.up:\n            result -= 1.0\n        if self.down:\n            result += 1.0\n        return result\n\n    def is_any_direction(self) -> bool:\n        return self.up or self.down or self.left or self.right\n\n\nclass InputSource(Protocol):\n    \"\"\"Protocol for all input backends (keyboard, Magma Hub, etc.).\"\"\"\n    def poll(self) -> InputState: ...\n    def is_pressed(self, button: str) -> bool: ...\n",
  "input/keyboard.py": "from __future__ import annotations\n\nimport tkinter as tk\nfrom dataclasses import replace\n\nfrom texastoast.input.abstract import InputState\n\n\nclass KeyboardInput:\n    \"\"\"Keyboard input using tkinter key bindings.\"\"\"\n\n    def __init__(self, root: tk.Tk):\n        self._state = InputState()\n        self._root = root\n        # (sequence, funcid) pairs \u2014 unbind() wants the sequence, not the funcid.\n        self._bindings: list[tuple[str, str]] = []\n\n        key_map = {\n            \"Up\": \"up\", \"w\": \"up\", \"W\": \"up\",\n            \"Down\": \"down\", \"s\": \"down\", \"S\": \"down\",\n            \"Left\": \"left\", \"a\": \"left\", \"A\": \"left\",\n            \"Right\": \"right\", \"d\": \"right\", \"D\": \"right\",\n            \"z\": \"a\", \"Z\": \"a\", \"Return\": \"a\",\n            \"x\": \"b\", \"X\": \"b\", \"BackSpace\": \"b\",\n            \"Escape\": \"start\", \"p\": \"start\", \"P\": \"start\",\n            \"Shift_L\": \"select\", \"Shift_R\": \"select\",\n        }\n\n        # Several keys map to the same button. Tracking which of them are down,\n        # rather than a single bool, keeps the button held until the last one is\n        # released \u2014 otherwise tapping 'a' would cancel a held Left arrow.\n        self._held: dict[str, set[str]] = {}\n\n        for key, button in key_map.items():\n            press_seq = f\"<KeyPress-{key}>\"\n            release_seq = f\"<KeyRelease-{key}>\"\n            press_id = root.bind(press_seq, lambda e, k=key, b=button: self._press(k, b))\n            release_id = root.bind(release_seq, lambda e, k=key, b=button: self._release(k, b))\n            self._bindings.append((press_seq, press_id))\n            self._bindings.append((release_seq, release_id))\n\n    def _press(self, key: str, button: str):\n        self._held.setdefault(button, set()).add(key)\n        setattr(self._state, button, True)\n\n    def _release(self, key: str, button: str):\n        keys = self._held.get(button)\n        if keys is not None:\n            keys.discard(key)\n        setattr(self._state, button, bool(keys))\n\n    def poll(self) -> InputState:\n        \"\"\"A snapshot of the current button state.\n\n        A copy, not the live object: callers keep the previous frame's state to\n        detect a fresh press, which is impossible if every poll hands back the\n        same instance.\n        \"\"\"\n        return replace(self._state)\n\n    def is_pressed(self, button: str) -> bool:\n        return getattr(self._state, button, False)\n\n    def destroy(self):\n        \"\"\"Remove every key binding this input source installed.\"\"\"\n        for sequence, funcid in self._bindings:\n            try:\n                self._root.unbind(sequence, funcid)\n            except Exception:\n                # Root may already be torn down.\n                pass\n        self._bindings.clear()\n        self._held.clear()\n        self._state = InputState()\n",
  "input/magma_hub.py": "\"\"\"I2C input adapter \u2014 reads controller state from I2C hub devices.\"\"\"\n\nfrom __future__ import annotations\n\nimport logging\nfrom dataclasses import replace\n\nfrom texastoast.i2c.hub import MagmaHub\nfrom texastoast.input.abstract import InputState\n\nlogger = logging.getLogger(__name__)\n\n_IDLE = InputState()\n\n\nclass MagmaHubInput:\n    \"\"\"Reads controller input from a Magma Hub I2C device.\n\n    Implements the same poll()/is_pressed() interface as KeyboardInput,\n    so games can swap between them seamlessly.\n    \"\"\"\n\n    def __init__(self, hub: MagmaHub, controller_index: int = 0):\n        self._hub = hub\n        self._controller_index = controller_index\n        self._state = InputState()\n\n    @property\n    def hub(self) -> MagmaHub:\n        return self._hub\n\n    @property\n    def connected(self) -> bool:\n        return self._hub.connected\n\n    def poll(self) -> InputState:\n        \"\"\"A snapshot of this controller's state. See :meth:`KeyboardInput.poll`.\"\"\"\n        controllers = self._hub.poll()\n        if self._controller_index < len(controllers):\n            cs = controllers[self._controller_index]\n            self._state = InputState(\n                up=cs.up,\n                down=cs.down,\n                left=cs.left,\n                right=cs.right,\n                a=cs.a,\n                b=cs.b,\n                start=cs.start,\n                select=cs.select,\n            )\n        else:\n            # The hub stopped reporting this controller. Keeping the last state\n            # would leave whatever was held at that moment stuck down forever.\n            self._state = InputState()\n        return replace(self._state)\n\n    def is_pressed(self, button: str) -> bool:\n        return getattr(self._state, button, False)\n\n\nclass CompositeInput:\n    \"\"\"Combines keyboard and Magma Hub input \u2014 falls back to keyboard\n    when no hub is connected.\"\"\"\n\n    def __init__(self, keyboard=None, hub_input: MagmaHubInput | None = None):\n        self._keyboard = keyboard\n        self._hub_input = hub_input\n\n    @property\n    def active_source(self) -> str:\n        if self._hub_input and self._hub_input.connected:\n            return \"magma_hub\"\n        if self._keyboard:\n            return \"keyboard\"\n        return \"none\"\n\n    def poll(self) -> InputState:\n        if self._hub_input and self._hub_input.connected:\n            return self._hub_input.poll()\n        if self._keyboard:\n            return self._keyboard.poll()\n        return replace(_IDLE)\n\n    def is_pressed(self, button: str) -> bool:\n        if self._hub_input and self._hub_input.connected:\n            return self._hub_input.is_pressed(button)\n        if self._keyboard:\n            return self._keyboard.is_pressed(button)\n        return False\n",
  "mgs.py": "\"\"\"The magmascript binding \u2014 the engine, exposed to ``.mgs`` scripts.\n\nThis module imports nothing from magmascript. It is discovered through the\n``magmascript.domains`` entry point declared in ``pyproject.toml``, so installing\ntexastoast alongside magmascript is all it takes::\n\n    g = texastoast.game({\"title\": \"Demo\", \"width\": 400, \"height\": 300})\n    r = texastoast.renderer(g, 400, 300)\n    kb = texastoast.keyboard(g)\n    ...\n    g.start()\n\nThe domain is named ``texastoast`` rather than ``toast``: magmascript's CLI\nalready spells ``magmascript toast <target>`` for clearing caches, and one name\nmeaning two things in one tool is worse than a longer one.\n\nTwo things shape the API here, both properties of the language rather than of\nthe engine:\n\n* **Options arrive as a dict, not keyword arguments.** MagmaScript has no\n  keyword-argument syntax, so ``toast.game({\"title\": ...})`` is the closest\n  spelling to ``Game(title=...)``. Unknown keys are an error rather than a\n  silent default, since a typo is otherwise invisible.\n* **Engine objects are returned bare.** The interpreter reaches host objects\n  through ``getattr``, which is all a plain class needs. Nothing here wraps or\n  copies; ``p.x`` in a script is the same attribute as ``entity.x`` in Python.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Any\n\nfrom texastoast.core.game import Game\nfrom texastoast.input.keyboard import KeyboardInput\nfrom texastoast.render.canvas import CanvasRenderer\nfrom texastoast.ui.dialogue import DialogueBox\nfrom texastoast.ui.hud import HUD\nfrom texastoast.ui.menu import Menu\nfrom texastoast.world.entity import Entity\nfrom texastoast.world.tilemap import TileMap\n\n__all__ = [\"TexastoastDomain\"]\n\n\ndef _num(value: Any) -> Any:\n    \"\"\"Unwrap an Asthenosphere fixed-width value to a plain number.\n\n    A script may reasonably write ``i32(100)`` for a speed. Handing that\n    straight to the engine breaks the first ``math.hypot`` it reaches, so\n    widths are stripped at the boundary. Duck-typed rather than imported:\n    this module does not depend on magmascript.\n    \"\"\"\n    spec = getattr(value, \"spec\", None)\n    if spec is not None and hasattr(value, \"value\"):\n        return value.value\n    return value\n\n\ndef _options(opts: Any, allowed: dict[str, Any], who: str) -> dict[str, Any]:\n    \"\"\"Validate an options dict against ``allowed`` and fill in the defaults.\"\"\"\n    if opts is None:\n        return dict(allowed)\n    if not isinstance(opts, dict):\n        raise TypeError(f\"{who} expected a dict of options, got {type(opts).__name__}\")\n\n    unknown = set(opts) - set(allowed)\n    if unknown:\n        raise ValueError(\n            f\"{who} got unknown option{'s' if len(unknown) > 1 else ''} \"\n            f\"{', '.join(repr(k) for k in sorted(unknown))}. \"\n            f\"Valid options: {', '.join(sorted(allowed))}\"\n        )\n\n    resolved = dict(allowed)\n    resolved.update({key: _num(value) for key, value in opts.items()})\n    return resolved\n\n\nclass TexastoastDomain:\n    \"\"\"texastoast, exposed to ``.mgs`` scripts as ``texastoast`` (or ``tt``).\n\n    Constructing this does nothing \u2014 no window, no canvas, no tkinter import\n    beyond what the package already did. magmascript builds a domain's client\n    lazily, but even so, a REPL that merely mentions the domain must not open a\n    window.\n    \"\"\"\n\n    def __init__(self, config: Any = None):\n        # magmascript passes its own Config. texastoast has no use for it.\n        self._config = config\n\n    # \u2500\u2500 core \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    def game(self, opts: dict | None = None) -> Game:\n        o = _options(opts, {\n            \"title\": \"texastoast\",\n            \"width\": 640,\n            \"height\": 480,\n            \"fps\": 30,\n            \"max_consecutive_errors\": 10,\n        }, \"texastoast.game()\")\n        return Game(\n            title=str(o[\"title\"]),\n            width=int(o[\"width\"]),\n            height=int(o[\"height\"]),\n            fps=int(o[\"fps\"]),\n            max_consecutive_errors=int(o[\"max_consecutive_errors\"]),\n        )\n\n    def renderer(self, game: Game, width: int, height: int) -> CanvasRenderer:\n        return CanvasRenderer(game.canvas, int(_num(width)), int(_num(height)))\n\n    def keyboard(self, game: Game) -> KeyboardInput:\n        return KeyboardInput(game.root)\n\n    # \u2500\u2500 world \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    def tilemap(self, grid, tile_size: int = 16, solid=None) -> TileMap:\n        \"\"\"Build a tile map. ``solid`` is any list of solid tile ids.\"\"\"\n        rows = [[int(_num(cell)) for cell in row] for row in grid]\n        solid_ids = [int(_num(t)) for t in solid] if solid is not None else None\n        return TileMap(rows, tile_size=int(_num(tile_size)), solid_tiles=solid_ids)\n\n    def entity(self, opts: dict | None = None) -> Entity:\n        o = _options(opts, {\n            \"x\": 0, \"y\": 0, \"width\": 16, \"height\": 16, \"speed\": 1.0,\n        }, \"texastoast.entity()\")\n        return Entity(\n            x=float(o[\"x\"]), y=float(o[\"y\"]),\n            width=float(o[\"width\"]), height=float(o[\"height\"]),\n            speed=float(o[\"speed\"]),\n        )\n\n    # \u2500\u2500 ui \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n\n    def dialogue(self, game: Game, opts: dict | None = None) -> DialogueBox:\n        o = _options(opts, {\n            \"width\": 640, \"height\": 480, \"box_height\": 100,\n            \"padding\": 12, \"speed\": 0.03,\n        }, \"texastoast.dialogue()\")\n        return DialogueBox(\n            game.canvas,\n            width=int(o[\"width\"]), height=int(o[\"height\"]),\n            box_height=int(o[\"box_height\"]), padding=int(o[\"padding\"]),\n            speed=float(o[\"speed\"]),\n        )\n\n    def menu(self, game: Game, opts: dict | None = None) -> Menu:\n        o = _options(opts, {\n            \"width\": 640, \"height\": 480,\n            \"selected_color\": \"#e94560\", \"normal_color\": \"#ffffff\",\n            \"disabled_color\": \"#555555\", \"item_padding\": 8,\n        }, \"texastoast.menu()\")\n        return Menu(\n            game.canvas,\n            width=int(o[\"width\"]), height=int(o[\"height\"]),\n            selected_color=str(o[\"selected_color\"]),\n            normal_color=str(o[\"normal_color\"]),\n            disabled_color=str(o[\"disabled_color\"]),\n            item_padding=int(o[\"item_padding\"]),\n        )\n\n    def hud(self, game: Game, opts: dict | None = None) -> HUD:\n        o = _options(opts, {\"width\": 640, \"height\": 480, \"padding\": 8},\n                     \"texastoast.hud()\")\n        return HUD(\n            game.canvas,\n            width=int(o[\"width\"]), height=int(o[\"height\"]),\n            padding=int(o[\"padding\"]),\n        )\n\n    def version(self) -> str:\n        from texastoast import __version__\n        return __version__\n\n    def __repr__(self) -> str:\n        return \"<texastoast>\"\n",
  "render/__init__.py": "from texastoast.render.camera import Camera\n\n__all__ = [\"CanvasRenderer\", \"Camera\", \"SpriteSheet\", \"load_image\"]\n\n\ndef __getattr__(name):\n    if name == \"CanvasRenderer\":\n        from texastoast.render.canvas import CanvasRenderer\n        return CanvasRenderer\n    if name == \"SpriteSheet\":\n        from texastoast.render.sprite import SpriteSheet\n        return SpriteSheet\n    if name == \"load_image\":\n        from texastoast.render.sprite import load_image\n        return load_image\n    raise AttributeError(f\"module {__name__!r} has no attribute {name!r}\")\n",
  "render/camera.py": "from __future__ import annotations\n\nimport math\nfrom dataclasses import dataclass\n\n# ``smoothing`` was calibrated as a per-frame lerp factor at this rate, so it\n# is the rate at which the old and new behaviour agree exactly.\nREFERENCE_FPS = 30.0\n\n\n@dataclass\nclass Camera:\n    \"\"\"Viewport camera with smooth following.\"\"\"\n    width: int = 640\n    height: int = 480\n    x: float = 0.0\n    y: float = 0.0\n    smoothing: float = 0.1\n\n    def follow(self, target_x: float, target_y: float, map_width: int = 0,\n               map_height: int = 0, dt: float | None = None):\n        \"\"\"Ease the viewport towards ``(target_x, target_y)``.\n\n        Pass the frame's ``dt`` to make the easing frame-rate independent:\n        ``smoothing`` is then a per-frame factor *at 30 fps*, converted to a\n        time constant, so the camera lags by the same distance at any fps.\n        Omitting ``dt`` keeps the old per-frame behaviour.\n        \"\"\"\n        target_cx = target_x - self.width / 2\n        target_cy = target_y - self.height / 2\n\n        alpha = self._alpha(dt)\n        self.x += (target_cx - self.x) * alpha\n        self.y += (target_cy - self.y) * alpha\n\n        if map_width > 0:\n            self.x = max(0, min(self.x, map_width - self.width))\n        if map_height > 0:\n            self.y = max(0, min(self.y, map_height - self.height))\n\n    def _alpha(self, dt: float | None) -> float:\n        \"\"\"The lerp factor for this frame.\n\n        Applying ``smoothing`` once per frame means the camera converges twice\n        as fast at 60 fps as at 30. Treating it as a per-second time constant\n        and integrating over ``dt`` removes the dependency.\n        \"\"\"\n        s = min(max(self.smoothing, 0.0), 1.0)\n        if dt is None:\n            return s\n        if s >= 1.0:\n            return 1.0\n        if s <= 0.0 or dt <= 0.0:\n            return 0.0\n        rate = -math.log(1.0 - s) * REFERENCE_FPS\n        return 1.0 - math.exp(-rate * dt)\n\n    def set_position(self, x: float, y: float):\n        self.x = x\n        self.y = y\n\n    def world_to_screen(self, wx: float, wy: float) -> tuple[float, float]:\n        return wx - self.x, wy - self.y\n\n    def screen_to_world(self, sx: float, sy: float) -> tuple[float, float]:\n        return sx + self.x, sy + self.y\n\n    def is_visible(self, x: float, y: float, w: float, h: float) -> bool:\n        return not (x + w < self.x or x > self.x + self.width or\n                    y + h < self.y or y > self.y + self.height)\n",
  "render/canvas.py": "from __future__ import annotations\n\nimport tkinter as tk\nfrom collections.abc import Iterable\n\nfrom texastoast.render.camera import Camera\n\n\nclass CanvasRenderer:\n    \"\"\"Renders tiles, sprites, and layers onto a tkinter Canvas.\"\"\"\n\n    def __init__(self, canvas: tk.Canvas, width: int, height: int):\n        self._canvas = canvas\n        self._width = width\n        self._height = height\n        self._camera = Camera(width, height)\n\n    @property\n    def canvas(self) -> tk.Canvas:\n        return self._canvas\n\n    @property\n    def camera(self) -> Camera:\n        return self._camera\n\n    def clear(self):\n        self._canvas.delete(\"all\")\n\n    def draw_tilemap(self, tilemap, tile_colors: dict[int, str],\n                     skip_tiles: Iterable[int] | None = None):\n        \"\"\"Draw the visible region of ``tilemap``.\n\n        A tile is drawn when its id has an entry in ``tile_colors``; ids that\n        are absent are left transparent, as are any listed in ``skip_tiles``.\n        \"\"\"\n        cam = self._camera\n        ts = tilemap.tile_size\n        skip = set(skip_tiles) if skip_tiles is not None else None\n\n        start_col = max(0, int(cam.x // ts))\n        end_col = min(tilemap.cols, int((cam.x + self._width) // ts) + 2)\n        start_row = max(0, int(cam.y // ts))\n        end_row = min(tilemap.rows, int((cam.y + self._height) // ts) + 2)\n\n        for row in range(start_row, end_row):\n            for col in range(start_col, end_col):\n                tile_id = tilemap.get(col, row)\n                if skip is not None and tile_id in skip:\n                    continue\n                color = tile_colors.get(tile_id)\n                if color is None:\n                    continue\n                x1 = col * ts - cam.x\n                y1 = row * ts - cam.y\n                x2 = x1 + ts\n                y2 = y1 + ts\n                self._canvas.create_rectangle(x1, y1, x2, y2, fill=color, outline=\"\")\n\n    def draw_rect(self, x: float, y: float, w: float, h: float, color: str, tag: str = \"\"):\n        cam = self._camera\n        sx = x - cam.x\n        sy = y - cam.y\n        self._canvas.create_rectangle(sx, sy, sx + w, sy + h, fill=color, outline=\"\", tags=tag)\n\n    def draw_image(self, x: float, y: float, image: tk.PhotoImage, anchor: str = \"nw\", tag: str = \"\"):\n        cam = self._camera\n        sx = x - cam.x\n        sy = y - cam.y\n        self._canvas.create_image(sx, sy, image=image, anchor=anchor, tags=tag)\n\n    def draw_text(self, x: float, y: float, text: str, **kwargs):\n        cam = self._camera\n        sx = x - cam.x\n        sy = y - cam.y\n        defaults = {\"fill\": \"#ffffff\", \"anchor\": \"nw\", \"font\": (\"Courier\", 10)}\n        defaults.update(kwargs)\n        self._canvas.create_text(sx, sy, text=text, **defaults)\n\n    def draw_hud_text(self, x: float, y: float, text: str, **kwargs):\n        \"\"\"Draw text at screen-space coordinates (ignores camera).\"\"\"\n        defaults = {\"fill\": \"#ffffff\", \"anchor\": \"nw\", \"font\": (\"Courier\", 10)}\n        defaults.update(kwargs)\n        self._canvas.create_text(x, y, text=text, **defaults)\n",
  "render/sprite.py": "from __future__ import annotations\n\nimport tkinter as tk\nfrom pathlib import Path\n\ntry:\n    from PIL import Image, ImageTk\n    HAS_PIL = True\nexcept ImportError:\n    HAS_PIL = False\n\n\nclass SpriteSheet:\n    \"\"\"Loads a sprite sheet PNG and extracts individual frames.\n\n    Supports two backends:\n    - PIL/Pillow (preferred) \u2014 proper pixel-accurate cropping\n    - tkinter PhotoImage \u2014 fallback with copy() region extraction\n    \"\"\"\n\n    def __init__(self, path: str | Path, frame_width: int, frame_height: int):\n        self._path = Path(path)\n        self._frame_width = frame_width\n        self._frame_height = frame_height\n        self._image: tk.PhotoImage | None = None\n        self._pil_image: object | None = None\n        self._frames: dict[tuple[int, int], tk.PhotoImage] = {}\n        self._root = None\n\n    def load(self, root) -> tk.PhotoImage:\n        if self._image is not None:\n            return self._image\n\n        self._root = root\n\n        if HAS_PIL:\n            self._pil_image = Image.open(self._path)\n            self._image = ImageTk.PhotoImage(self._pil_image)\n        else:\n            self._image = tk.PhotoImage(file=str(self._path))\n\n        return self._image\n\n    def get_frame(self, root, col: int, row: int) -> tk.PhotoImage:\n        key = (col, row)\n        if key in self._frames:\n            return self._frames[key]\n\n        if self._image is None:\n            self.load(root)\n\n        if col < 0 or row < 0 or col >= self.cols or row >= self.rows:\n            raise ValueError(\n                f\"Frame ({col}, {row}) out of bounds \"\n                f\"(sheet is {self.cols}x{self.rows} frames)\"\n            )\n\n        if HAS_PIL and self._pil_image is not None:\n            frame = self._extract_pil_frame(col, row)\n        else:\n            frame = self._extract_tk_frame(col, row)\n\n        self._frames[key] = frame\n        return frame\n\n    def _extract_pil_frame(self, col: int, row: int) -> tk.PhotoImage:\n        x = col * self._frame_width\n        y = row * self._frame_height\n        box = (x, y, x + self._frame_width, y + self._frame_height)\n        frame_pil = self._pil_image.crop(box)\n        return ImageTk.PhotoImage(frame_pil)\n\n    def _extract_tk_frame(self, col: int, row: int) -> tk.PhotoImage:\n        x = col * self._frame_width\n        y = row * self._frame_height\n        # tkinter PhotoImage copy with region: copy region x1 y1 x2 y2\n        frame = self._image.copy(\n            f\"{x} {y} {x + self._frame_width} {y + self._frame_height}\"\n        )\n        return frame\n\n    @property\n    def frame_width(self) -> int:\n        return self._frame_width\n\n    @property\n    def frame_height(self) -> int:\n        return self._frame_height\n\n    @property\n    def cols(self) -> int:\n        if self._image is None:\n            return 0\n        width = self._image.width() if hasattr(self._image, 'width') else 0\n        return width // self._frame_width if self._frame_width > 0 else 0\n\n    @property\n    def rows(self) -> int:\n        if self._image is None:\n            return 0\n        height = self._image.height() if hasattr(self._image, 'height') else 0\n        return height // self._frame_height if self._frame_height > 0 else 0\n\n    @property\n    def total_frames(self) -> int:\n        return self.cols * self.rows\n\n\ndef load_image(root, path: str | Path) -> tk.PhotoImage:\n    if HAS_PIL:\n        img = Image.open(path)\n        return ImageTk.PhotoImage(img)\n    return tk.PhotoImage(file=str(path))\n",
  "ui/__init__.py": "from texastoast.ui.dialogue import DialogueBox\nfrom texastoast.ui.hud import HUD\nfrom texastoast.ui.menu import Menu\n\n__all__ = [\"DialogueBox\", \"Menu\", \"HUD\"]\n",
  "ui/dialogue.py": "from __future__ import annotations\n\nimport tkinter as tk\nfrom collections.abc import Callable\n\n\nclass DialogueBox:\n    \"\"\"Canvas-based dialogue box with typewriter text and portrait support.\n\n    Drawing is frame-driven, like :class:`~texastoast.ui.hud.HUD`: call\n    :meth:`update` from the game's update function and :meth:`render` from its\n    render function. A renderer that clears the canvas each frame would\n    otherwise wipe the box off screen while the box still believes it is up.\n    \"\"\"\n\n    def __init__(\n        self,\n        canvas: tk.Canvas,\n        width: int = 640,\n        height: int = 480,\n        box_height: int = 100,\n        padding: int = 12,\n        font: tuple = (\"Courier\", 12),\n        speed: float = 0.03,\n    ):\n        self._canvas = canvas\n        self._width = width\n        self._height = height\n        self._box_height = box_height\n        self._padding = padding\n        self._font = font\n        self._speed = speed\n\n        self._active = False\n        self._full_text = \"\"\n        self._char_index = 0\n        self._elapsed = 0.0\n        self._on_complete: Callable | None = None\n        self._speaker = \"\"\n        self._waiting = False\n        self._tag = \"dialogue\"\n\n    @property\n    def active(self) -> bool:\n        return self._active\n\n    @property\n    def waiting(self) -> bool:\n        \"\"\"True once the full text is on screen and a dismiss will close it.\"\"\"\n        return self._waiting\n\n    @property\n    def displayed(self) -> str:\n        return self._full_text[:self._char_index]\n\n    def show(self, text: str, speaker: str = \"\", on_complete: Callable | None = None):\n        self._full_text = text\n        self._speaker = speaker\n        self._on_complete = on_complete\n        self._char_index = 0\n        self._elapsed = 0.0\n        self._active = True\n        # Empty text has nothing to type, so it is immediately dismissable.\n        self._waiting = not text\n\n    def update(self, dt: float):\n        \"\"\"Advance the typewriter by ``dt`` seconds.\"\"\"\n        if not self._active or self._waiting:\n            return\n\n        if self._speed <= 0:\n            self._char_index = len(self._full_text)\n        else:\n            self._elapsed += dt\n            revealed = int(self._elapsed / self._speed)\n            if revealed > self._char_index:\n                self._char_index = min(revealed, len(self._full_text))\n\n        if self._char_index >= len(self._full_text):\n            self._waiting = True\n\n    def dismiss(self):\n        \"\"\"Skip to the end of the text, or close the box if it is already there.\"\"\"\n        if not self._active:\n            return\n        if self._waiting:\n            self._active = False\n            self._clear()\n            if self._on_complete:\n                self._on_complete()\n        else:\n            self._char_index = len(self._full_text)\n            self._waiting = True\n\n    def render(self):\n        \"\"\"Draw the box. Safe to call every frame, active or not.\"\"\"\n        self._canvas.delete(self._tag)\n        if not self._active:\n            return\n\n        x1 = self._padding\n        y1 = self._height - self._box_height - self._padding\n        x2 = self._width - self._padding\n        y2 = self._height - self._padding\n\n        self._canvas.create_rectangle(x1, y1, x2, y2,\n                                      fill=\"#000000\", outline=\"#ffffff\",\n                                      width=2, tags=self._tag)\n\n        if self._speaker:\n            self._canvas.create_text(\n                x1 + self._padding, y1 + 4,\n                text=self._speaker, fill=\"#e94560\",\n                font=(\"Courier\", 10, \"bold\"), anchor=tk.NW,\n                tags=self._tag,\n            )\n\n        text_x = x1 + self._padding\n        text_y = y1 + (self._padding + 28 if self._speaker else self._padding + 14)\n\n        self._canvas.create_text(\n            text_x, text_y,\n            text=self.displayed, fill=\"#ffffff\",\n            font=self._font, anchor=tk.NW,\n            width=self._width - self._padding * 4,\n            tags=self._tag,\n        )\n\n        if self._waiting:\n            self._canvas.create_text(\n                self._width - self._padding * 2,\n                self._height - self._padding * 2,\n                text=\"[A] continue\", fill=\"#aaaaaa\",\n                font=(\"Courier\", 9), anchor=tk.SE,\n                tags=self._tag,\n            )\n\n    def _clear(self):\n        self._canvas.delete(self._tag)\n",
  "ui/hud.py": "from __future__ import annotations\n\nimport tkinter as tk\nfrom dataclasses import dataclass\n\n\n@dataclass\nclass HUDStat:\n    label: str\n    value: float = 0\n    max_value: float = 100\n    color: str = \"#e94560\"\n    show_bar: bool = True\n    show_text: bool = True\n\n\nclass HUD:\n    \"\"\"Canvas-based heads-up display overlay for score, health, etc.\"\"\"\n\n    def __init__(\n        self,\n        canvas: tk.Canvas,\n        width: int = 640,\n        height: int = 480,\n        font: tuple = (\"Courier\", 10),\n        padding: int = 8,\n    ):\n        self._canvas = canvas\n        self._width = width\n        self._height = height\n        self._font = font\n        self._padding = padding\n        self._tag = \"hud\"\n        self._stats: dict[str, HUDStat] = {}\n        self._custom_texts: dict[str, tuple[str, float, float, dict]] = {}\n\n    def add_stat(self, key: str, label: str, value: float = 100,\n                 max_value: float = 100, color: str = \"#e94560\"):\n        self._stats[key] = HUDStat(label=label, max_value=max_value, color=color)\n        self.set_stat(key, value)\n\n    def set_stat(self, key: str, value: float):\n        \"\"\"Set a stat, clamped to ``[0, max_value]``.\"\"\"\n        if key in self._stats:\n            self._stats[key].value = max(0, min(value, self._stats[key].max_value))\n\n    def add_text(self, key: str, text: str, x: float, y: float, **kwargs):\n        defaults = {\"fill\": \"#ffffff\", \"font\": self._font, \"anchor\": tk.NW}\n        defaults.update(kwargs)\n        self._custom_texts[key] = (text, x, y, defaults)\n\n    def remove_text(self, key: str):\n        self._custom_texts.pop(key, None)\n\n    def set_text(self, key: str, text: str):\n        if key in self._custom_texts:\n            old = self._custom_texts[key]\n            self._custom_texts[key] = (text, old[1], old[2], old[3])\n\n    def clear(self):\n        self._stats.clear()\n        self._custom_texts.clear()\n        self._canvas.delete(self._tag)\n\n    def render(self):\n        self._canvas.delete(self._tag)\n        self._render_stats()\n        self._render_texts()\n\n    def _render_stats(self):\n        x = self._padding\n        y = self._padding\n        bar_width = 120\n        bar_height = 10\n        line_height = 20\n\n        for stat in self._stats.values():\n            if stat.show_text:\n                self._canvas.create_text(\n                    x, y, text=stat.label, fill=\"#cccccc\",\n                    font=self._font, anchor=tk.NW,\n                    tags=self._tag,\n                )\n                value_text = f\"{int(stat.value)}/{int(stat.max_value)}\"\n                self._canvas.create_text(\n                    x + bar_width + 8, y, text=value_text, fill=\"#aaaaaa\",\n                    font=self._font, anchor=tk.NW,\n                    tags=self._tag,\n                )\n\n            if stat.show_bar:\n                bar_x = x\n                bar_y = y + (14 if stat.show_text else 0)\n                ratio = stat.value / stat.max_value if stat.max_value > 0 else 0\n                self._canvas.create_rectangle(\n                    bar_x, bar_y, bar_x + bar_width, bar_y + bar_height,\n                    fill=\"#333333\", outline=\"#555555\",\n                    tags=self._tag,\n                )\n                if ratio > 0:\n                    self._canvas.create_rectangle(\n                        bar_x, bar_y, bar_x + bar_width * ratio, bar_y + bar_height,\n                        fill=stat.color, outline=\"\",\n                        tags=self._tag,\n                    )\n\n            y += line_height + (bar_height + 4 if stat.show_bar else 0)\n\n    def _render_texts(self):\n        for text, x, y, opts in self._custom_texts.values():\n            self._canvas.create_text(x, y, text=text, tags=self._tag, **opts)\n",
  "ui/menu.py": "from __future__ import annotations\n\nimport tkinter as tk\nfrom collections.abc import Callable\n\n\nclass Menu:\n    \"\"\"Canvas-based selectable menu with keyboard/controller navigation.\n\n    Drawing is frame-driven, like :class:`~texastoast.ui.hud.HUD`: call\n    :meth:`render` from the game's render function. A renderer that clears the\n    canvas each frame would otherwise wipe the menu off screen while the menu\n    still believes it is up.\n    \"\"\"\n\n    def __init__(\n        self,\n        canvas: tk.Canvas,\n        width: int = 640,\n        height: int = 480,\n        font: tuple = (\"Courier\", 14),\n        selected_color: str = \"#e94560\",\n        normal_color: str = \"#ffffff\",\n        disabled_color: str = \"#555555\",\n        item_padding: int = 8,\n    ):\n        self._canvas = canvas\n        self._width = width\n        self._height = height\n        self._font = font\n        self._selected_color = selected_color\n        self._normal_color = normal_color\n        self._disabled_color = disabled_color\n        self._item_padding = item_padding\n\n        self._active = False\n        self._items: list[dict] = []\n        self._selected = 0\n        self._on_select: Callable[[int, str], None] | None = None\n        self._on_cancel: Callable | None = None\n        self._tag = \"menu\"\n        self._title = \"\"\n\n    @property\n    def active(self) -> bool:\n        return self._active\n\n    @property\n    def selected_index(self) -> int:\n        return self._selected\n\n    def show(\n        self,\n        items: list[str],\n        on_select: Callable[[int, str], None] | None = None,\n        on_cancel: Callable | None = None,\n        title: str = \"\",\n        selected: int = 0,\n    ):\n        if not items:\n            return\n        self._items = [{\"label\": label, \"enabled\": True} for label in items]\n        self._selected = max(0, min(selected, len(self._items) - 1))\n        self._snap_to_enabled()\n        self._on_select = on_select\n        self._on_cancel = on_cancel\n        self._title = title\n        self._active = True\n\n    def hide(self):\n        self._active = False\n        self._canvas.delete(self._tag)\n\n    def move_up(self):\n        if not self._active or not self._items:\n            return\n        new = self._selected\n        while new > 0:\n            new -= 1\n            if self._items[new][\"enabled\"]:\n                self._selected = new\n                return\n\n    def move_down(self):\n        if not self._active or not self._items:\n            return\n        new = self._selected\n        while new < len(self._items) - 1:\n            new += 1\n            if self._items[new][\"enabled\"]:\n                self._selected = new\n                return\n\n    def confirm(self):\n        if not self._active or not self._items:\n            return\n        item = self._items[self._selected]\n        if not item[\"enabled\"]:\n            return\n        self.hide()\n        if self._on_select:\n            self._on_select(self._selected, item[\"label\"])\n\n    def cancel(self):\n        if not self._active:\n            return\n        self.hide()\n        if self._on_cancel:\n            self._on_cancel()\n\n    def set_enabled(self, index: int, enabled: bool):\n        if 0 <= index < len(self._items):\n            self._items[index][\"enabled\"] = enabled\n            if self._active:\n                self._snap_to_enabled()\n\n    def _snap_to_enabled(self):\n        if not self._items:\n            return\n        if self._items[self._selected][\"enabled\"]:\n            return\n        for i in range(len(self._items)):\n            if self._items[i][\"enabled\"]:\n                self._selected = i\n                return\n\n    def render(self):\n        \"\"\"Draw the menu. Safe to call every frame, active or not.\"\"\"\n        self._canvas.delete(self._tag)\n        if not self._active:\n            return\n\n        menu_width = 280\n        item_height = 32\n        total_h = len(self._items) * item_height + self._item_padding * 2\n        if self._title:\n            total_h += 28\n\n        cx = self._width / 2\n        cy = self._height / 2\n        x1 = cx - menu_width / 2\n        y1 = cy - total_h / 2\n        x2 = cx + menu_width / 2\n        y2 = cy + total_h / 2\n\n        self._canvas.create_rectangle(x1 - 4, y1 - 4, x2 + 4, y2 + 4,\n                                      fill=\"#000000\", outline=\"#ffffff\",\n                                      width=2, tags=self._tag)\n\n        y = y1 + self._item_padding\n\n        if self._title:\n            self._canvas.create_text(\n                cx, y + 10, text=self._title,\n                fill=self._normal_color, font=(\"Courier\", 11, \"bold\"),\n                tags=self._tag,\n            )\n            y += 28\n\n        for i, item in enumerate(self._items):\n            is_selected = (i == self._selected)\n            is_enabled = item[\"enabled\"]\n\n            if is_selected and is_enabled:\n                self._canvas.create_rectangle(\n                    x1 + 4, y, x2 - 4, y + item_height,\n                    fill=\"#331111\", outline=\"\",\n                    tags=self._tag,\n                )\n                color = self._selected_color\n                prefix = \"> \"\n            else:\n                color = self._normal_color if is_enabled else self._disabled_color\n                prefix = \"  \"\n\n            self._canvas.create_text(\n                x1 + self._item_padding + 8, y + item_height / 2,\n                text=prefix + item[\"label\"], fill=color,\n                font=self._font, anchor=tk.W,\n                tags=self._tag,\n            )\n            y += item_height\n",
  "world/__init__.py": "from texastoast.world.collision import AABB, check_tile_collision\nfrom texastoast.world.entity import Entity\nfrom texastoast.world.tilemap import TileMap\n\n__all__ = [\"TileMap\", \"AABB\", \"check_tile_collision\", \"Entity\"]\n",
  "world/collision.py": "from __future__ import annotations\n\nimport math\nfrom dataclasses import dataclass\n\n# Boxes are half-open: a box at x with width w covers [x, x + w). A box whose\n# right edge lands exactly on a tile boundary does not occupy the tile beyond\n# it, which is what makes flush contact stable.\n_EPS = 1e-9\n\n# Safety valve for absurd velocities. Movement is split into sub-steps of at\n# most one tile so nothing can pass through a wall; past this many sub-steps we\n# stop subdividing, and tunneling becomes possible again.\nMAX_SUBSTEPS = 256\n\n\n@dataclass\nclass AABB:\n    x: float\n    y: float\n    width: float\n    height: float\n\n    @property\n    def left(self) -> float:\n        return self.x\n\n    @property\n    def right(self) -> float:\n        return self.x + self.width\n\n    @property\n    def top(self) -> float:\n        return self.y\n\n    @property\n    def bottom(self) -> float:\n        return self.y + self.height\n\n    def intersects(self, other: AABB) -> bool:\n        return not (self.right <= other.left or self.left >= other.right or\n                    self.bottom <= other.top or self.top >= other.bottom)\n\n    def contains_point(self, px: float, py: float) -> bool:\n        return self.left <= px <= self.right and self.top <= py <= self.bottom\n\n\ndef _tile_span(lo: float, hi: float, tile_size: int) -> tuple[int, int]:\n    \"\"\"Inclusive range of tile indices covered by the half-open span [lo, hi).\"\"\"\n    first = int(math.floor(lo / tile_size))\n    last = int(math.floor((hi - _EPS) / tile_size))\n    return first, max(first, last)\n\n\ndef _substeps(velocity_x: float, velocity_y: float, tile_size: int) -> int:\n    \"\"\"Number of sub-steps needed to keep each step within one tile.\"\"\"\n    longest = max(abs(velocity_x), abs(velocity_y))\n    if longest <= tile_size:\n        return 1\n    return min(MAX_SUBSTEPS, int(math.ceil(longest / tile_size)))\n\n\ndef _resolve_x(x: float, y: float, w: float, h: float, tilemap, dx: float) -> float:\n    ts = tilemap.tile_size\n    next_x = x + dx\n    if dx > 0:\n        col = _tile_span(next_x, next_x + w, ts)[1]\n    else:\n        col = _tile_span(next_x, next_x + w, ts)[0]\n\n    first_row, last_row = _tile_span(y, y + h, ts)\n    for row in range(first_row, last_row + 1):\n        if tilemap.is_solid(col, row):\n            if dx > 0:\n                flush = col * ts - w\n                return flush if flush > x else x\n            flush = (col + 1) * ts\n            return flush if flush < x else x\n    return next_x\n\n\ndef _resolve_y(x: float, y: float, w: float, h: float, tilemap, dy: float) -> float:\n    ts = tilemap.tile_size\n    next_y = y + dy\n    if dy > 0:\n        row = _tile_span(next_y, next_y + h, ts)[1]\n    else:\n        row = _tile_span(next_y, next_y + h, ts)[0]\n\n    first_col, last_col = _tile_span(x, x + w, ts)\n    for col in range(first_col, last_col + 1):\n        if tilemap.is_solid(col, row):\n            if dy > 0:\n                flush = row * ts - h\n                return flush if flush > y else y\n            flush = (row + 1) * ts\n            return flush if flush < y else y\n    return next_y\n\n\ndef check_tile_collision(\n    x: float, y: float, w: float, h: float, tilemap,\n    velocity_x: float = 0.0, velocity_y: float = 0.0,\n) -> tuple[float, float]:\n    \"\"\"Resolve movement of a box against a tile map's solid tiles.\n\n    ``velocity_x``/``velocity_y`` are a displacement for this frame, not a rate.\n\n    Each axis is resolved independently so an entity slides along a wall\n    instead of sticking to it. A blocked axis snaps flush against the wall face\n    rather than reverting to its starting position, and movement is split into\n    sub-steps of at most one tile so a fast-moving box cannot pass through a\n    wall (up to ``MAX_SUBSTEPS``).\n\n    Returns the corrected ``(x, y)`` position.\n    \"\"\"\n    ts = tilemap.tile_size\n    if ts <= 0:\n        return x + velocity_x, y + velocity_y\n\n    steps = _substeps(velocity_x, velocity_y, ts)\n    step_x = velocity_x / steps\n    step_y = velocity_y / steps\n\n    for _ in range(steps):\n        if step_x:\n            x = _resolve_x(x, y, w, h, tilemap, step_x)\n        if step_y:\n            y = _resolve_y(x, y, w, h, tilemap, step_y)\n\n    return x, y\n",
  "world/entity.py": "from __future__ import annotations\n\nimport math\n\nfrom texastoast.world.collision import AABB, check_tile_collision\n\n\nclass Entity:\n    \"\"\"Base game entity with position, velocity, and hitbox.\n\n    ``speed`` is in pixels per second. :meth:`move` takes the frame's delta\n    time, so movement is frame-rate independent.\n    \"\"\"\n\n    def __init__(\n        self,\n        x: float = 0,\n        y: float = 0,\n        width: float = 16,\n        height: float = 16,\n        speed: float = 1.0,\n    ):\n        self.x = x\n        self.y = y\n        self.width = width\n        self.height = height\n        self.speed = speed\n        self.vel_x = 0.0\n        self.vel_y = 0.0\n\n    @property\n    def aabb(self) -> AABB:\n        return AABB(self.x, self.y, self.width, self.height)\n\n    @property\n    def center_x(self) -> float:\n        return self.x + self.width / 2\n\n    @property\n    def center_y(self) -> float:\n        return self.y + self.height / 2\n\n    def move(self, dx: float, dy: float, dt: float, tilemap=None):\n        \"\"\"Move along the direction vector ``(dx, dy)`` for ``dt`` seconds.\n\n        ``dx``/``dy`` are direction components, normally -1, 0 or 1 (see\n        :attr:`~texastoast.input.abstract.InputState.dx`). A diagonal is\n        normalized, so moving on both axes is no faster than moving on one.\n\n        Sets :attr:`vel_x`/:attr:`vel_y` to the resulting velocity in pixels\n        per second. When ``tilemap`` is given, movement is resolved against its\n        solid tiles.\n        \"\"\"\n        magnitude = math.hypot(dx, dy)\n        if magnitude > 1.0:\n            dx /= magnitude\n            dy /= magnitude\n\n        self.vel_x = dx * self.speed\n        self.vel_y = dy * self.speed\n\n        step_x = self.vel_x * dt\n        step_y = self.vel_y * dt\n\n        if tilemap is not None:\n            self.x, self.y = check_tile_collision(\n                self.x, self.y, self.width, self.height,\n                tilemap, step_x, step_y,\n            )\n        else:\n            self.x += step_x\n            self.y += step_y\n\n    def update(self, dt: float):\n        pass\n\n    def collides_with(self, other: Entity) -> bool:\n        return self.aabb.intersects(other.aabb)\n",
  "world/tilemap.py": "from __future__ import annotations\n\nimport json\nfrom collections.abc import Iterable\nfrom pathlib import Path\n\n\nclass TileMap:\n    \"\"\"2D tile grid with collision data.\"\"\"\n\n    def __init__(\n        self,\n        grid: list[list[int]],\n        tile_size: int = 16,\n        solid_tiles: Iterable[int] | None = None,\n    ):\n        self._grid = grid\n        self._tile_size = tile_size\n        # Any iterable, not just a set: a list is the natural spelling from\n        # JSON and from languages without a set literal, and membership is all\n        # this is ever used for.\n        self._solid_tiles = set(solid_tiles) if solid_tiles is not None else set()\n\n    @classmethod\n    def from_file(cls, path: str | Path, tile_size: int | None = None, solid_tiles: Iterable[int] | None = None) -> TileMap:\n        with open(path) as f:\n            data = json.load(f)\n        ts = tile_size if tile_size is not None else data.get(\"tile_size\", 16)\n        st = solid_tiles if solid_tiles is not None else set(data.get(\"solid_tiles\", []))\n        return cls(data[\"grid\"], tile_size=ts, solid_tiles=st)\n\n    @property\n    def solid_tiles(self) -> set[int]:\n        return self._solid_tiles\n\n    @property\n    def grid(self) -> list[list[int]]:\n        return self._grid\n\n    @property\n    def tile_size(self) -> int:\n        return self._tile_size\n\n    @property\n    def rows(self) -> int:\n        return len(self._grid)\n\n    @property\n    def cols(self) -> int:\n        if not self._grid:\n            return 0\n        return max(len(row) for row in self._grid)\n\n    @property\n    def width(self) -> int:\n        return self.cols * self._tile_size\n\n    @property\n    def height(self) -> int:\n        return self.rows * self._tile_size\n\n    def get(self, col: int, row: int) -> int:\n        if 0 <= row < self.rows and 0 <= col < len(self._grid[row]):\n            return self._grid[row][col]\n        return -1\n\n    def set(self, col: int, row: int, tile_id: int):\n        if 0 <= row < self.rows and 0 <= col < len(self._grid[row]):\n            self._grid[row][col] = tile_id\n\n    def is_solid(self, col: int, row: int) -> bool:\n        tile_id = self.get(col, row)\n        if tile_id == -1:\n            return True\n        return tile_id in self._solid_tiles\n\n    def is_solid_at(self, world_x: float, world_y: float) -> bool:\n        col = int(world_x // self._tile_size)\n        row = int(world_y // self._tile_size)\n        return self.is_solid(col, row)\n\n    def to_grid_coords(self, world_x: float, world_y: float) -> tuple[int, int]:\n        return int(world_x // self._tile_size), int(world_y // self._tile_size)\n\n    def save(self, path: str | Path, solid_tiles: Iterable[int] | None = None):\n        data = {\n            \"grid\": self._grid,\n            \"tile_size\": self._tile_size,\n            \"solid_tiles\": list(solid_tiles if solid_tiles is not None else self._solid_tiles),\n        }\n        with open(path, \"w\") as f:\n            json.dump(data, f, indent=2)\n",
};
// END GENERATED TEXASTOAST FILES
  const EXAMPLES = [
    "hello_world",
    "tilemap_demo",
    "rpg_demo",
    "game_template",
  ];

  // Derived from the generated object above.
  const TT_FILES = Object.keys(TT_FILE_CONTENTS);

  // ----------------------------------------------------------
  // DOM references
  // ----------------------------------------------------------

  const editorPanel = document.getElementById("editor-panel");
  const divider = document.getElementById("divider");
  const consoleLog = document.getElementById("console-log");
  const loadingBar = document.getElementById("loading-bar");
  const statusInterp = document.getElementById("status-interp");
  const statusExample = document.getElementById("status-example");
  const statusReady = document.getElementById("status-ready");
  const exampleSelect = document.getElementById("example-select");
  const runBtn = document.getElementById("run");
  const stopBtn = document.getElementById("stop");
  const resetBtn = document.getElementById("reset");
  const aboutBtn = document.getElementById("about-btn");
  const aboutModal = document.getElementById("about-modal");
  const gameCanvas = document.getElementById("game-canvas");
  const gameTitle = document.getElementById("game-title");
  const focusHint = document.getElementById("focus-hint");
  const ctx = gameCanvas.getContext("2d");

  // ----------------------------------------------------------
  // State
  // ----------------------------------------------------------

  let pyodide = null;
  let editorView = null;
  let originalCode = "";

  // ----------------------------------------------------------
  // Logging helpers
  // ----------------------------------------------------------

  function appendConsole(msg, cls) {
    const el = document.createElement("div");
    el.className = `console-line ${cls}`;
    el.textContent = msg;
    consoleLog.appendChild(el);
    // Keep the strip bounded — a runaway print loop should not grow the DOM
    // without limit.
    while (consoleLog.childElementCount > 500) {
      consoleLog.removeChild(consoleLog.firstChild);
    }
    consoleLog.scrollTop = consoleLog.scrollHeight;
  }

  function logInit(msg) { appendConsole(msg, "log"); }
  function logError(msg) { appendConsole(msg, "error"); }

  function setStatus(html) {
    statusReady.innerHTML = html;
  }

  function setLoading(visible) {
    loadingBar.classList.toggle("hidden", !visible);
  }

  // ----------------------------------------------------------
  // The JS host: canvas, display list, timers, keyboard
  // ----------------------------------------------------------
  // tkinter is retained-mode — per-tag delete() has to filter a list of
  // drawn items, so the host keeps one and repaints it (coalesced with
  // requestAnimationFrame). Append order is z-order, which reproduces
  // tkinter's delete-and-recreate layering.

  const host = {
    items: [],
    nextId: 1,
    dirty: false,
    bg: "#1a1a2e",
    timers: new Set(),
    dispatch: null,      // PyProxy: (kind, keysym) -> None
    closeHandler: null,  // PyProxy: () -> None (the window-close protocol)

    // -- lifecycle --

    initCanvas(w, h, bg) {
      gameCanvas.width = w;
      gameCanvas.height = h;
      // Integer upscale that fits the panel; CSS max-width caps it further.
      gameCanvas.style.width = `${w * 2}px`;
      host.bg = bg;
      host.items = [];
      host.repaint();
    },

    setTitle(text) {
      gameTitle.textContent = text;
    },

    setDispatch(proxy) {
      host.dispatch = proxy;
      gameCanvas.focus();
    },

    setCloseHandler(proxy) {
      host.closeHandler = proxy;
    },

    requestClose() {
      // The Stop button and pre-run cleanup are "clicking the X": the
      // close protocol runs Game.quit -> loop.stop -> teardown -> destroy.
      if (host.closeHandler) {
        try {
          host.closeHandler();
        } catch (err) {
          logError(`Error while stopping: ${err.message}`);
          host.reset();
        }
      } else {
        host.reset();
      }
    },

    teardown() {
      for (const id of host.timers) clearTimeout(id);
      host.timers.clear();
      host.dispatch = null;
      host.closeHandler = null;
      host.items = [];
      host.repaint();
    },

    reset() {
      host.teardown();
      gameTitle.textContent = "";
    },

    // -- scheduling --

    after(ms, cb) {
      const id = setTimeout(() => {
        host.timers.delete(id);
        try {
          cb();
        } catch (err) {
          logError(`Error in game loop: ${err.message}`);
        }
      }, ms);
      host.timers.add(id);
      return id;
    },

    afterCancel(id) {
      clearTimeout(id);
      host.timers.delete(id);
    },

    // -- display list --

    deleteItems(spec) {
      if (spec === "all") {
        host.items = [];
      } else {
        const tags = spec.split(",");
        host.items = host.items.filter(
          (it) => !tags.some((t) => it.tags.includes(t))
        );
      }
      host.repaint();
    },

    rect(x1, y1, x2, y2, fill, outline, width, tags) {
      const id = host.nextId++;
      host.items.push({ kind: "rect", id, x1, y1, x2, y2, fill, outline, width, tags });
      host.repaint();
      return id;
    },

    text(x, y, text, fill, font, anchor, wrapWidth, tags) {
      const id = host.nextId++;
      host.items.push({ kind: "text", id, x, y, text, fill, font, anchor, wrapWidth, tags });
      host.repaint();
      return id;
    },

    repaint() {
      if (host.dirty) return;
      host.dirty = true;
      requestAnimationFrame(() => {
        host.dirty = false;
        host.paint();
      });
    },

    paint() {
      ctx.fillStyle = host.bg;
      ctx.fillRect(0, 0, gameCanvas.width, gameCanvas.height);
      for (const it of host.items) {
        if (it.kind === "rect") {
          if (it.fill) {
            ctx.fillStyle = it.fill;
            ctx.fillRect(it.x1, it.y1, it.x2 - it.x1, it.y2 - it.y1);
          }
          if (it.outline) {
            ctx.strokeStyle = it.outline;
            ctx.lineWidth = it.width || 1;
            ctx.strokeRect(it.x1, it.y1, it.x2 - it.x1, it.y2 - it.y1);
          }
        } else if (it.kind === "text") {
          host.paintText(it);
        }
      }
    },

    paintText(it) {
      ctx.font = it.font;
      ctx.fillStyle = it.fill || "#ffffff";
      const anchor = it.anchor || "center";
      ctx.textAlign = anchor.includes("w") ? "left"
        : anchor.includes("e") ? "right" : "center";
      const baseline = anchor.includes("n") ? "top"
        : anchor.includes("s") ? "bottom" : "middle";
      ctx.textBaseline = baseline;

      const fontPx = parseInt((it.font.match(/(\d+)px/) || [0, 13])[1], 10);
      const lineHeight = Math.round(fontPx * 1.2);

      let lines = String(it.text).split("\n");
      if (it.wrapWidth > 0) {
        lines = lines.flatMap((line) => host.wrapLine(line, it.wrapWidth));
      }

      lines.forEach((line, i) => {
        let y = it.y;
        if (baseline === "top") y += i * lineHeight;
        else if (baseline === "bottom") y -= (lines.length - 1 - i) * lineHeight;
        else y += (i - (lines.length - 1) / 2) * lineHeight;
        ctx.fillText(line, it.x, y);
      });
    },

    wrapLine(line, maxWidth) {
      const words = line.split(" ");
      const out = [];
      let current = "";
      for (const word of words) {
        const candidate = current ? `${current} ${word}` : word;
        if (current && ctx.measureText(candidate).width > maxWidth) {
          out.push(current);
          current = word;
        } else {
          current = candidate;
        }
      }
      out.push(current);
      return out;
    },

    // -- console bridge (Python stdout/stderr land here) --

    consoleLine(text, isErr) {
      appendConsole(text, isErr ? "error" : "log");
    },
  };

  globalThis.__ttHost = host;

  // ----------------------------------------------------------
  // Keyboard: browser events -> tkinter keysyms
  // ----------------------------------------------------------

  const PREVENT_DEFAULT_KEYS = new Set([
    "ArrowUp", "ArrowDown", "ArrowLeft", "ArrowRight", " ", "Backspace",
  ]);

  function keysymFor(e) {
    switch (e.key) {
      case "ArrowUp": return "Up";
      case "ArrowDown": return "Down";
      case "ArrowLeft": return "Left";
      case "ArrowRight": return "Right";
      case "Enter": return "Return";
      case "Backspace": return "BackSpace";
      case "Escape": return "Escape";
      case " ": return "space";
      case "Shift": return e.location === 2 ? "Shift_R" : "Shift_L";
      case "Control": return e.location === 2 ? "Control_R" : "Control_L";
      case "Alt": return e.location === 2 ? "Alt_R" : "Alt_L";
      default: return e.key;
    }
  }

  function initKeyboard() {
    const forward = (kind) => (e) => {
      if (PREVENT_DEFAULT_KEYS.has(e.key)) e.preventDefault();
      if (!host.dispatch) return;
      try {
        host.dispatch(kind, keysymFor(e));
      } catch (err) {
        logError(`Error in key handler: ${err.message}`);
      }
    };
    gameCanvas.addEventListener("keydown", forward("press"));
    gameCanvas.addEventListener("keyup", forward("release"));
    gameCanvas.addEventListener("focus", () => focusHint.classList.add("hidden"));
    gameCanvas.addEventListener("blur", () => {
      if (host.dispatch) focusHint.classList.remove("hidden");
    });
  }

  // ----------------------------------------------------------
  // CodeMirror 6 setup (independent of Pyodide)
  // ----------------------------------------------------------

  async function initEditor() {
    logInit("Loading CodeMirror...");

    const stateMod = await import("https://esm.sh/@codemirror/state@6");
    const viewMod = await import("https://esm.sh/@codemirror/view@6");
    const pyMod = await import("https://esm.sh/@codemirror/lang-python@6");
    const themeMod = await import("https://esm.sh/@codemirror/theme-one-dark@6");

    const EditorState = stateMod.EditorState;
    const EditorView = viewMod.EditorView;
    const keymap = viewMod.keymap;
    const python = pyMod.python;
    const oneDark = themeMod.oneDark;

    if (!EditorState || !EditorView || !keymap || !python || !oneDark) {
      throw new Error(
        `CodeMirror import failed: EditorState=${!!EditorState}, EditorView=${!!EditorView}, keymap=${!!keymap}, python=${!!python}, oneDark=${!!oneDark}`
      );
    }

    const runKeymap = keymap.of([
      { key: "Ctrl-Enter", run: () => { runCode(); return true; } },
      { key: "Cmd-Enter", run: () => { runCode(); return true; } },
    ]);

    const state = EditorState.create({
      doc: '# Write texastoast code here — or pick an example above\nprint("hello from texastoast")\n',
      extensions: [
        python(),
        oneDark,
        runKeymap,
        EditorView.lineWrapping,
        EditorView.theme({
          "&": { height: "100%" },
          ".cm-scroller": { overflow: "auto" },
        }),
      ],
    });

    editorView = new EditorView({
      state,
      parent: editorPanel,
    });

    logInit("CodeMirror ready");
  }

  // ----------------------------------------------------------
  // Pyodide bootstrap (sequential, step-by-step)
  // ----------------------------------------------------------

  async function initPyodide() {
    logInit("Loading Pyodide runtime...");
    setStatus('<span class="dot loading"></span>loading');

    const script = document.createElement("script");
    script.src = PYODIDE_CDN;
    document.head.appendChild(script);
    await new Promise((resolve, reject) => {
      script.onload = resolve;
      script.onerror = () => reject(new Error("Failed to load Pyodide CDN script"));
    });
    logInit("Pyodide script loaded");

    logInit("Initializing WASM runtime (this may take a moment)...");
    const loadFn = window.loadPyodide;
    if (typeof loadFn !== "function") {
      throw new Error(
        "loadPyodide not found on window. Pyodide script may not have loaded correctly."
      );
    }
    pyodide = await loadFn({
      indexURL: "https://cdn.jsdelivr.net/pyodide/v0.25.1/full/",
    });
    logInit("WASM runtime ready");

    if (TT_FILES.length === 0) {
      throw new Error(
        "No embedded texastoast modules — run scripts/sync-texastoast-playground.py"
      );
    }

    logInit("Creating filesystem...");
    const fs = pyodide.FS;
    const dirs = [
      "/home/pyodide/texastoast",
      // Subpackage dirs come from the embedded paths, so a new subpackage
      // needs no edit here.
      ...TT_FILES.filter((n) => n.includes("/")).map(
        (n) => `/home/pyodide/texastoast/${n.slice(0, n.lastIndexOf("/"))}`
      ),
    ];
    for (const dir of dirs) {
      if (!fs.analyzePath(dir).exists) {
        fs.mkdirTree(dir);
      }
    }
    logInit("Directories created");

    for (const name of TT_FILES) {
      fs.writeFile(`/home/pyodide/texastoast/${name}`, TT_FILE_CONTENTS[name]);
    }
    logInit(`All ${TT_FILES.length} texastoast modules installed`);

    logInit("Installing tkinter shim...");
    const shimResp = await fetch("tt_tk_shim.py");
    if (!shimResp.ok) throw new Error(`Failed to fetch tt_tk_shim.py: HTTP ${shimResp.status}`);
    fs.writeFile("/home/pyodide/tkinter.py", await shimResp.text());
    logInit("tkinter shim installed");

    logInit("Configuring Python path...");
    pyodide.runPython(`
import sys
sys.path.insert(0, '/home/pyodide')
`);

    logInit("Testing Python imports...");
    try {
      pyodide.runPython(`
import tkinter
assert getattr(tkinter, "__tt_shim__", False), "stdlib tkinter shadowed the shim"
from texastoast import Game, CanvasRenderer, Entity, TileMap, KeyboardInput
import texastoast
print(f"Import OK: texastoast {texastoast.__version__} with browser tkinter shim")
`);
    } catch (e) {
      throw new Error(`Python import failed: ${e.message}`);
    }
    logInit("Python imports verified");

    logInit("Installing runner...");
    pyodide.runPython(RUNNER_CODE);
    logInit("Runner installed");

    setLoading(false);
    statusInterp.innerHTML = '<span class="dot ok"></span>Pyodide';
    setStatus('<span class="dot ok"></span>Ready');
  }

  // ----------------------------------------------------------
  // Runner code (executed inside Pyodide)
  // ----------------------------------------------------------
  // Unlike a capture-and-return runner, game output arrives asynchronously
  // from after() callbacks long after _run returns — so stdout/stderr are
  // permanently redirected to the console strip, and logging (which the
  // GameLoop uses for error reporting) writes to stderr.

  const RUNNER_CODE = `
import logging
import sys
import traceback

import js

# Module-level alias: inside a class body, js.__ttHost would be
# name-mangled to js._HostWriter__ttHost and miss the JS global.
_HOST = js.__ttHost


class _HostWriter:
    def __init__(self, is_err):
        self._is_err = is_err
        self._buf = ""

    def write(self, text):
        self._buf += str(text)
        while "\\n" in self._buf:
            line, self._buf = self._buf.split("\\n", 1)
            _HOST.consoleLine(line, self._is_err)
        return len(text)

    def flush(self):
        if self._buf:
            _HOST.consoleLine(self._buf, self._is_err)
            self._buf = ""


sys.stdout = _HostWriter(False)
sys.stderr = _HostWriter(True)
logging.basicConfig(stream=sys.stderr, level=logging.INFO)


def _run(code):
    scope = {"__name__": "__main__"}
    try:
        exec(compile(code, "<playground>", "exec"), scope)
    except BaseException:
        traceback.print_exc()
    finally:
        sys.stdout.flush()
        sys.stderr.flush()
`;

  // ----------------------------------------------------------
  // Code execution
  // ----------------------------------------------------------

  function runCode() {
    if (!pyodide) {
      logError("Interpreter not ready — still loading");
      return;
    }

    const code = editorView.state.doc.toString();
    if (!code.trim()) {
      logError("No code to run");
      return;
    }

    // Stop any running game the same way the window X would.
    host.requestClose();

    consoleLog.innerHTML = "";
    logInit("Running...");

    try {
      pyodide.globals.set("__code", code);
      pyodide.runPython("_run(__code)");
      if (host.dispatch) {
        logInit("Game running — click the canvas and use the keyboard");
        gameCanvas.focus();
      } else {
        logInit("Done");
      }
    } catch (err) {
      logError(`Error: ${err.message}`);
    }
  }

  function stopCode() {
    host.requestClose();
    logInit("Stopped");
  }

  // ----------------------------------------------------------
  // Example loading
  // ----------------------------------------------------------

  async function loadExample(name) {
    statusExample.textContent = `example: ${name}`;
    try {
      const resp = await fetch(`examples/${name}.py`);
      if (!resp.ok) throw new Error(`HTTP ${resp.status}`);
      const code = await resp.text();
      originalCode = code;
      editorView.dispatch({
        changes: { from: 0, to: editorView.state.doc.length, insert: code },
      });
    } catch (err) {
      logError(`Failed to load example: ${err.message}`);
    }
  }

  function populateExamples() {
    exampleSelect.innerHTML = "";
    const placeholder = document.createElement("option");
    placeholder.value = "";
    placeholder.textContent = "— select example —";
    placeholder.disabled = true;
    placeholder.selected = true;
    exampleSelect.appendChild(placeholder);

    EXAMPLES.forEach((name) => {
      const opt = document.createElement("option");
      opt.value = name;
      opt.textContent = name;
      exampleSelect.appendChild(opt);
    });
  }

  // ----------------------------------------------------------
  // Split pane
  // ----------------------------------------------------------

  function initSplitPane() {
    let isDragging = false;
    const outputPanel = document.getElementById("output-panel");

    divider.addEventListener("mousedown", (e) => {
      isDragging = true;
      e.preventDefault();
      document.body.style.cursor = "col-resize";
      document.body.style.userSelect = "none";
    });

    document.addEventListener("mousemove", (e) => {
      if (!isDragging) return;
      const container = document.querySelector("main");
      const rect = container.getBoundingClientRect();
      const pct = ((e.clientX - rect.left) / rect.width) * 100;
      const clamped = Math.min(Math.max(pct, 20), 80);
      editorPanel.style.flex = `0 0 ${clamped}%`;
      outputPanel.style.flex = `0 0 ${100 - clamped}%`;
    });

    document.addEventListener("mouseup", () => {
      if (!isDragging) return;
      isDragging = false;
      document.body.style.cursor = "";
      document.body.style.userSelect = "";
    });
  }

  // ----------------------------------------------------------
  // About modal
  // ----------------------------------------------------------

  function initAboutModal() {
    const modalClose = aboutModal.querySelector(".modal-close");

    aboutBtn.addEventListener("click", () => {
      aboutModal.showModal();
    });

    modalClose.addEventListener("click", () => {
      aboutModal.close();
    });

    aboutModal.addEventListener("click", (e) => {
      if (e.target === aboutModal) aboutModal.close();
    });
  }

  // ----------------------------------------------------------
  // Event wiring
  // ----------------------------------------------------------

  function wireEvents() {
    runBtn.addEventListener("click", runCode);
    stopBtn.addEventListener("click", stopCode);
    resetBtn.addEventListener("click", () => {
      host.requestClose();
      editorView.dispatch({
        changes: {
          from: 0,
          to: editorView.state.doc.length,
          insert: originalCode,
        },
      });
      consoleLog.innerHTML = "";
    });

    exampleSelect.addEventListener("change", (e) => {
      if (e.target.value) loadExample(e.target.value);
    });
  }

  // ----------------------------------------------------------
  // Init — sequential, with per-step error handling
  // ----------------------------------------------------------

  try {
    // Phase 1: Editor + keyboard (independent of Pyodide)
    await initEditor();
    initKeyboard();

    // Phase 2: Pyodide (the heavy lift)
    await initPyodide();

    // Phase 3: UI wiring
    populateExamples();
    wireEvents();
    initSplitPane();
    initAboutModal();

    // Start with the smallest playable example in the editor.
    exampleSelect.value = "hello_world";
    await loadExample("hello_world");
  } catch (err) {
    console.error("Playground init failed:", err);
    setLoading(false);
    setStatus('<span class="dot err"></span>Error');
    logError(`Init failed: ${err.message}`);
    if (err.stack) {
      logError(err.stack.split("\n").slice(0, 3).join(" | "));
    }
  }
})();
